---
import { Image } from 'astro:assets';
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProcessTimeline from '../../components/ProcessTimeline.astro';
import emotionalMap from '../../assets/emotional-map.png';
import thyroxine from '../../assets/kaira-thyroxine.png';
import logoLandscape from '../../assets/kaira-logo-landscape.png';
import iconSamples from '../../assets/kaira-icon-samples.png';
import gradient from '../../assets/kaira-gradient.png';
import patternMolecule from '../../assets/kaira-pattern-molecule.png';
import logicFlowchart from '../../assets/logic-flowchart.png';
import insightBlueprint from '../../assets/insight-blueprint.png';
import csHero from '../../assets/cs-hero.png';
import kairaFinal from '../../assets/kaira-final.png';

// Full-resolution PDF exports served from public/ for the zoom lightbox.
const LOGIC_FLOWCHART_FULL = '/assets/logic-flowchart-full.png';
const LOGIC_FULL = '/assets/logic-full.png';
const INSIGHT_FULL = '/assets/insight-blueprint-full.png';

// Interactive prototype of the logging flow (Figma). Session token stripped.
const protoUrl =
  'https://www.figma.com/proto/DJqxPf65KHQ1HJ0S31eYxr/Food-Logging-with-Kaira?node-id=1732-14602&scaling=scale-down&content-scaling=fixed&starting-point-node-id=1732%3A14602&page-id=0%3A1';
const figmaEmbed = `https://www.figma.com/embed?embed_host=share&url=${encodeURIComponent(protoUrl)}`;
---

<BaseLayout
  title="Food Logging with Kaira | Daksh Sharma"
  description="Case study: good logging feels like talking, not filling. Replacing a food-logging form with a conversation. Sole product designer, GoodFlip."
>
  <article class="cs">
    <a class="cs__back" href="/#work">← Back to work</a>

    <figure class="cs__hero">
      <Image
        src={csHero}
        alt="Kaira app: a conversational meal log and the resulting insight card with calories, macros, and a meal score, around the Kaira hexagon mark"
        widths={[900, 1440, 2000, 3360]}
        sizes="min(64rem, 92vw)"
        loading="eager"
        fetchpriority="high"
      />
    </figure>

    {/* 0. ANCHOR */}
    <header class="cs__anchor">
      <p class="cs__kicker">Case study</p>
      <h1 class="cs__title">Food Logging with Kaira</h1>
      <p class="cs__hook">
        Good logging feels like talking, not filling. So I stopped designing a
        form.
      </p>
      <p class="cs__role">
        Sole product designer, GoodFlip. A metabolic health platform helping
        thousands of people manage chronic conditions.
      </p>

      <dl class="cs__stats">
        <div class="stat">
          <dd class="stat__value">92.5%</dd>
          <dt class="stat__label">positive feedback on Kaira’s responses</dt>
          <dd class="stat__base">172 of 186 rated responses</dd>
        </div>
        <div class="stat">
          <dd class="stat__value">~9 in 10</dd>
          <dt class="stat__label">log with Kaira on their own, without a coach or diet plan</dt>
          <dd class="stat__base">from a backend export, ~900 Kaira users over 10 months</dd>
        </div>
      </dl>
    </header>

    {/* 1. THE BET */}
    <section class="beat">
      <h2 class="beat__h">Why we needed to build this</h2>
      <p class="beat__subline">
        Food logging was not a small feature. It was the one bet that pulled on
        acquisition, retention, positioning, and metabolic leverage at once.
      </p>
    </section>

    <ul class="bet-cards">
      <li class="bet-card">
        <h3 class="bet-card__label">Only coached users logged</h3>
        <p class="bet-card__body">
          The data showed food logging happened almost only where a coach pushed a
          user on a plan. Left on their own, people did not log.
        </p>
        <a class="bet-card__callback" href="#outcome">
          This is the exact thing Kaira later reversed. See the outcome.
        </a>
      </li>
      <li class="bet-card">
        <h3 class="bet-card__label">Nutrition is the leverage</h3>
        <p class="bet-card__body">
          Nutrition is a core pillar of metabolism, so a logging habit was
          leverage we were leaving on the table.
        </p>
      </li>
      <li class="bet-card">
        <h3 class="bet-card__label">The category demanded it</h3>
        <p class="bet-card__body">
          Competitors already had AI-powered logging and users expected it. We
          could not even market ourselves in the space without it.
        </p>
      </li>
      <li class="bet-card">
        <h3 class="bet-card__label">A wider top of funnel</h3>
        <p class="bet-card__body">
          Easy, low-effort logging is the kind of entry point that widens the top
          of the funnel and brings new users in.
        </p>
      </li>
      <li class="bet-card">
        <h3 class="bet-card__label">A reason to come back</h3>
        <p class="bet-card__body">
          A daily logging habit gives people a reason to return, which is exactly
          the retention we needed.
        </p>
      </li>
    </ul>

    {/* 1b. THE PITCH */}
    <section class="beat">
      <h2 class="beat__h">I pitched a chatbot, not another feature</h2>
      <p class="beat__subline">
        The ask was AI-powered food logging. I pitched something bigger, a
        chatbot, and we went with it for three reasons.
      </p>
    </section>

    <ul class="pitch-cards">
      <li class="pitch-card">
        <div class="pitch-card__top">
          <span class="pitch-card__icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false">
              <polygon points="8.4,12 6.7,14.94 3.3,14.94 1.6,12 3.3,9.06 6.7,9.06"/>
              <circle cx="19" cy="5.5" r="1.9"/>
              <circle cx="20.4" cy="12" r="1.9"/>
              <circle cx="19" cy="18.5" r="1.9"/>
              <path d="M8 10.6 17.2 6.3"/>
              <path d="M8.4 12 18.5 12"/>
              <path d="M8 13.4 17.2 17.7"/>
            </svg>
          </span>
        </div>
        <h3 class="pitch-card__label">Scalability</h3>
        <p class="pitch-card__body">
          A chatbot was not a one-off feature. It was a surface we could extend
          into any other part of the app later, so the effort compounded instead
          of ending at food.
        </p>
      </li>

      <li class="pitch-card">
        <div class="pitch-card__top">
          <span class="pitch-card__icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false">
              <polygon points="16,13 13,18.196 7,18.196 4,13 7,7.804 13,7.804"/>
              <path d="M18.5 3.2 19.4 5.3 21.5 6.2 19.4 7.1 18.5 9.2 17.6 7.1 15.5 6.2 17.6 5.3Z" fill="currentColor" stroke="none"/>
            </svg>
          </span>
        </div>
        <h3 class="pitch-card__label">A personality to market</h3>
        <p class="pitch-card__body">
          A chatbot could be given a character and a name, which handed marketing
          an angle a plain logging feature never could.
        </p>
      </li>

      <li class="pitch-card">
        <div class="pitch-card__top">
          <span class="pitch-card__icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false">
              <polygon points="12,14 9.5,18.33 4.5,18.33 2,14 4.5,9.67 9.5,9.67"/>
              <polygon points="22,10 19.5,14.33 14.5,14.33 12,10 14.5,5.67 19.5,5.67" opacity="0.5"/>
            </svg>
          </span>
          <span class="pitch-card__tag">Hypothesis</span>
        </div>
        <h3 class="pitch-card__label">Familiarity (our hypothesis)</h3>
        <p class="pitch-card__body">
          If people learned one Kaira experience for food, they would already know
          how to use Kaira everywhere else we took it. One learned interaction,
          reused across the product.
        </p>
      </li>
    </ul>

    {/* 2. PROCESS IN A NUTSHELL */}
    <section class="beat">
      <h2 class="beat__h">Process in a nutshell</h2>
      <p>
        Before the decisions, here is how I work by default. This is my operating
        system, not a checklist I ran once. The case study below is where I show
        which of these I leaned on hardest, which I skipped, and why.
      </p>
      <ProcessTimeline />
    </section>

    {/* 3. DECISION ONE */}
    <section class="beat">
      <p class="beat__eyebrow">Decision one</p>
      <h2 class="beat__h">Don’t build a form, study how people talk</h2>
    </section>

    <section class="study-banner reveal" aria-labelledby="study-headline">
      <p class="study-banner__eyebrow">The study</p>
      <h3 class="study-banner__headline" id="study-headline">
        I asked people to log yesterday, two different ways
      </h3>
      <p class="study-banner__setup">
        Before designing anything, I ran a quick study with people around me,
        asking each to recall their past day of meals in two conditions.
      </p>

      <div class="study-conditions">
        <div class="study-cond">
          <span class="study-cond__label">Free (open prompt)</span>
          <p class="study-cond__quote">“Describe everything you ate and did yesterday.”</p>
          <p class="study-cond__note">People narrated naturally, 12 to 58 seconds.</p>
        </div>
        <div class="study-cond">
          <span class="study-cond__label">Nudged (guided prompt)</span>
          <p class="study-cond__quote">“What did you have for lunch today?”</p>
          <p class="study-cond__note">
            People answered faster and more precisely, 10 to 20 seconds.
          </p>
        </div>
      </div>

      <p class="study-finding">
        Free speech was rich but dropped the exact fields I needed: quantity,
        timing, portion, supplement timing, sleep. A nudge recovered them.
        Neither alone worked.
      </p>

      <p class="study-aim">
        Aim: observe how people naturally speak when logging, to find the
        patterns and friction that should shape a fast, intuitive logging
        experience.
      </p>
    </section>

    <section class="beat">
      <p>
        The easy version of this project was a cleaner form. I did not start
        there. A chat is only as good as its grasp of how people speak, so I
        started by listening.
      </p>
      <p>
        I ran a small voice study with people around the office, two tasks each.
        First, describe everything you ate and did yesterday, in your own words.
        Then a single nudged prompt: what did you have for lunch today? The gap
        between those two answers was the whole project.
      </p>
    </section>

    <div class="cs__wide">
      <table class="ctable">
        <thead>
          <tr><th>What I heard</th><th>What it meant</th></tr>
        </thead>
        <tbody>
          <tr>
            <td>
              Open prompt: people narrate chronologically, mix languages, measure
              in bowls and “a little bit,” and skip quantity, timing, portion, and
              sleep. 12 to 58 seconds, often hesitant.
            </td>
            <td>Natural, but data-poor.</td>
          </tr>
          <tr>
            <td>
              Nudged prompt: one specific question, and people get faster, more
              confident, more structured. 10 to 20 seconds.
            </td>
            <td>Structured, but rigid if you ask too much.</td>
          </tr>
          <tr>
            <td><strong>The tension</strong></td>
            <td>
              Open voice is natural but data-poor. Nudged voice is structured but
              rigid. Neither works alone, the design has to be a hybrid: let
              people speak, then infer, and nudge only for the few things you
              cannot assume.
            </td>
          </tr>
        </tbody>
      </table>
    </div>

    <section class="beat">
      <p>
        Then the emotional side. Logging is not only a data problem, it is an
        emotional one. One of the ways people landed in the food logger was
        through their CGM journey. Someone sees a glucose reading, wants to know
        what caused it, and that curiosity is the doorway into logging. So the
        emotional map I built follows that CGM-triggered path, from curious and
        unsure when the reading first appears, to motivated, to bored once
        logging turns routine, to anxious when a bad number shows up, to guilty
        when they break the streak.
      </p>
    </section>

    <figure class="shot shot--hero">
      <Image
        class="shot__img"
        src={emotionalMap}
        alt="Emotional journey map: stages, user thoughts, emotions, and app touchpoints across the CGM-triggered path into logging"
        widths={[900, 1440, 2000]}
        sizes="(max-width: 44rem) 92vw, min(64rem, 92vw)"
      />
      <figcaption>
        The emotional journey I mapped for the CGM-triggered path into logging.
        This told me the flow had to meet people at each state, not just capture
        their data.
      </figcaption>
    </figure>

    {/* 4. DECISION TWO */}
    <section class="beat">
      <p class="beat__eyebrow">Decision two</p>
      <h2 class="beat__h">Infer, don’t interrogate</h2>
      <p>
        If speaking freely leaves gaps, the obvious fix is to ask more questions.
        That is also the fastest way to make people quit. So I built the
        opposite. Let people speak however they naturally do, then infer the
        missing structure instead of demanding it.
      </p>
      <p>
        I went through every field a log needs and split it in two. What Kaira
        must ask, and what she can safely assume. For everything assumable, I
        wrote the fallback logic behind it: pull from the user’s coach plan, then
        their own history, then population defaults, and reason from scratch only
        for the genuinely odd inputs.
      </p>
    </section>

    <figure class="shot shot--hero">
      <button
        class="zoomable"
        data-lightbox={LOGIC_FLOWCHART_FULL}
        data-caption="The logic I designed: let people speak, infer what I can, and ask gently only for what I cannot assume, without ever dead-ending on a vague answer."
        aria-label="Zoom the conversation logic diagram"
      >
        <Image
          class="shot__img"
          src={logicFlowchart}
          alt="Conversation logic flowchart: user speaks, Kaira detects meals, fills assumed fields, asks only for missing info, and confirms"
          widths={[900, 1584]}
          sizes="min(64rem, 92vw)"
        />
        <span class="zoomable__hint" aria-hidden="true">Tap to zoom</span>
      </button>
      <figcaption>
        The logic I designed: let people speak, infer what I can, and ask gently
        only for what I cannot assume, without ever dead-ending on a vague answer.
        The system is allowed to guess. It is never allowed to hide that it
        guessed.
        <button
          class="expand-link"
          data-lightbox={LOGIC_FULL}
          data-caption="The complete assumption-engine flow: what Kaira asks, what she infers, the food-database lookup, and the GPT fallback for unusual inputs."
        >
          See the full logic I handed to engineering →
        </button>
      </figcaption>
    </figure>

    <section class="beat">
      <p>
        There was exactly one field I refused to let it assume: meal type.
        Because people log at the end of the day, “poha and chai” could be
        breakfast or an evening snack, and getting that wrong quietly poisons the
        data. So Kaira always asks that one thing and assumes the rest. That
        single no is the design.
      </p>
      <p>
        I also designed a flow for logging several meals at once, for the person
        who dumps their whole day in one go. It was cut for dev effort before
        launch. Remember this one, because the data brings it back later.
      </p>
    </section>

    <section class="beat">
      <p>
        On top of the capture sat the part I care about most: what Kaira says
        back. I built the response to scale to how someone logged, whether a
        single item, a full meal, or a whole day, and to always end on the one
        thing a metabolic platform cannot leave out, the likely sugar spike. Same
        voice, right-sized to the input.
      </p>
    </section>

    <figure class="shot shot--support">
      <button
        class="zoomable"
        data-lightbox={INSIGHT_FULL}
        data-caption="The response blueprint behind that insight card up top: calorie, macro, spike risk, metabolic verdict, always ending on the likely sugar spike."
        aria-label="Zoom the meal-level insight logic"
      >
        <Image
          class="shot__img shot__img--clip"
          src={insightBlueprint}
          alt="Meal-level insight logic document: calorie summary, macro overview, spike risk, and metabolic verdict layers"
          widths={[700, 1200]}
          sizes="(max-width: 44rem) 92vw, 44rem"
        />
        <span class="zoomable__hint" aria-hidden="true">Tap to zoom</span>
      </button>
      <figcaption>
        The response blueprint behind that insight card up top: calorie, macro,
        spike risk, metabolic verdict, always ending on the likely sugar spike.
      </figcaption>
    </figure>

    {/* 5. GIVING KAIRA AN IDENTITY */}
    <section class="beat">
      <p class="beat__eyebrow">Decision three</p>
      <h2 class="beat__h">Giving Kaira an identity</h2>
    </section>

    <div class="acronym reveal">
      <p class="acronym__mark">KAIRA</p>
      <p class="acronym__full">
        <b>K</b>nowledgeable <b>AI</b> for <b>R</b>emission and <b>A</b>wareness
      </p>
    </div>

    <ol class="idtl">
      <li class="idtl__stage">
        <span class="idtl__num" aria-hidden="true">01</span>
        <h3 class="idtl__label">Rooted in the science</h3>
        <p class="idtl__line">
          Thyroxine, the hormone at the center of metabolism, is built on a
          hexagonal ring. The identity starts there.
        </p>
        <figure class="idtl__visual idtl__visual--pad">
          <Image src={thyroxine} alt="Thyroxine molecule structure showing its hexagonal rings" widths={[440, 880]} sizes="(max-width: 44rem) 92vw, 38rem" />
        </figure>
      </li>

      <li class="idtl__stage">
        <span class="idtl__num" aria-hidden="true">02</span>
        <h3 class="idtl__label">Built to be recognisable</h3>
        <p class="idtl__line">
          Competitive analysis showed AI marks trending busy and over-clever. I
          chose a simple hexagon anyone could recognise instantly.
        </p>
        <figure class="idtl__visual">
          <Image src={logoLandscape} alt="Competitive analysis grid of AI product logos" widths={[600, 1000]} sizes="(max-width: 44rem) 92vw, 38rem" />
        </figure>
      </li>

      <li class="idtl__stage">
        <span class="idtl__num" aria-hidden="true">03</span>
        <h3 class="idtl__label">Colour from our system</h3>
        <p class="idtl__line">
          The gradients came from our existing design system, so Kaira felt native
          to the product, not bolted on.
        </p>
        <figure class="idtl__visual">
          <Image src={gradient} alt="Kaira gradient swatches from the design system" widths={[600, 1000]} sizes="(max-width: 44rem) 92vw, 38rem" />
        </figure>
      </li>

      <li class="idtl__stage">
        <span class="idtl__num" aria-hidden="true">04</span>
        <h3 class="idtl__label">A pattern with the same DNA</h3>
        <p class="idtl__line">
          The surrounding pattern is drawn from the same thyroxine geometry, tying
          the whole identity to one idea.
        </p>
        <figure class="idtl__visual idtl__visual--band">
          <Image src={patternMolecule} alt="Kaira pattern built from repeating hexagonal molecule motifs" widths={[600, 1000]} sizes="(max-width: 44rem) 92vw, 38rem" />
        </figure>
      </li>

      <li class="idtl__stage idtl__stage--final">
        <span class="idtl__num" aria-hidden="true">05</span>
        <h3 class="idtl__label">The result</h3>
        <p class="idtl__line">One clean, scientific, recognisable mark.</p>
        <figure class="idtl__visual idtl__visual--final">
          <Image
            src={kairaFinal}
            alt="The final Kaira identity: the thyroxine-inspired hexagon icon, UI containers, animated patterns, and gradient buttons"
            widths={[600, 1000, 2000]}
            sizes="(max-width: 44rem) 92vw, 38rem"
            loading="lazy"
          />
        </figure>
      </li>
    </ol>

    <section class="beat">
      <h3 class="beat__sub">Other directions I explored</h3>
    </section>

    <figure class="shot shot--support alternates">
      <Image
        class="shot__img"
        src={iconSamples}
        alt="Grid of alternate Kaira hexagon logo explorations that were considered"
        widths={[500, 1000]}
        sizes="(max-width: 44rem) 92vw, 34rem"
      />
    </figure>

    {/* 6. SEE IT IN ACTION */}
    <section class="beat">
      <h2 class="beat__h">See it in action</h2>
      <p>Enough logic and stills. Here is the thing working, end to end.</p>
    </section>

    <figure class="shot shot--hero">
      <div class="proto">
        <iframe
          class="proto__frame"
          src={figmaEmbed}
          title="Interactive Figma prototype of the Kaira food logging flow"
          loading="lazy"
          allowfullscreen
        ></iframe>
      </div>
      <figcaption>
        A user speaks or types a meal however they like. Kaira parses it, fills
        the gaps with visible, editable assumptions, shows the breakdown and the
        metabolic read, and logs it the moment they confirm. Speak, glance, done.
      </figcaption>
    </figure>

    {/* THE HONEST PART (what we traded to go live) */}
    <section class="beat">
      <h2 class="beat__h">The honest part</h2>
      <p>
        The version I designed was richer than the version we shipped, and that
        was a deliberate call. To go live on time, we chose the leaner build we
        could stand behind now over the fuller one that would have held the
        launch. Shipping and learning beat polishing in private.
      </p>
      <p>
        Three things I had designed were held back for the first release. The
        rich, card-based meal breakdown, with its per-item cards and tables,
        shipped as standard text and table answers instead. The Meal Score, a
        single at-a-glance read of how good a meal was, was set aside. And batch
        logging, the flow for dumping a whole day at once, was deferred. Each was
        a conscious trade to get a working product into people's hands, not a
        limit on what could be built.
      </p>
      <p>
        I would rather name that plainly than pretend the shipped thing was the
        ideal thing. Knowing the difference, and choosing what to sacrifice under
        a real constraint, is the job.
      </p>
    </section>

    {/* 7. DECISION FOUR */}
    <section class="beat">
      <p class="beat__eyebrow">Decision four</p>
      <h2 class="beat__h">Ship it, then let the data prove me wrong</h2>
    </section>

    <aside class="research" aria-label="Research impact">
      <header class="research__head">
        <p class="research__eyebrow">The reckoning</p>
        <h3 class="research__title">I shipped it, then read 4,000 conversations</h3>
        <p class="research__lede">
          A behavioural audit of how people actually used Kaira, and where my
          design failed them.
        </p>
      </header>

      <ul class="research__method">
        <li>4,000 interactions · 168 users · Mar 11–31, 2026</li>
        <li>Sources: MoEngage events and full conversation audit logs</li>
        <li>Also ran AI-driven feedback calls (Bolna.ai) to hear users in their own words</li>
      </ul>

      <dl class="research__grid">
        <div class="rstat">
          <dd class="rstat__value">3.7%</dd>
          <dt class="rstat__label">of users had ever logged a meal</dt>
          <dd class="rstat__base">256 of 6,866 over 60 days</dd>
        </div>
        <div class="rstat">
          <dd class="rstat__value">63%</dd>
          <dt class="rstat__label">log several items in one go</dt>
          <dd class="rstat__base">106 of 168, March behaviour audit</dd>
        </div>
        <div class="rstat">
          <dd class="rstat__value">50%</dd>
          <dt class="rstat__label">of users hit extra round-trips</dt>
          <dd class="rstat__base">Kaira asking “how much?” or “what time?” (374 instances, 85 users)</dd>
        </div>
        <div class="rstat">
          <dd class="rstat__value">31%</dd>
          <dt class="rstat__label">dropped off after seeing their food analysed</dt>
          <dd class="rstat__base">before tapping confirm</dd>
        </div>
        <div class="rstat rstat--win">
          <dd class="rstat__value">92.5%</dd>
          <dt class="rstat__label">positive feedback on Kaira’s responses</dt>
          <dd class="rstat__base">172 of 186 rated responses</dd>
        </div>
      </dl>
    </aside>

    <div class="takeaways">
      <div class="takeaway">
        <p class="takeaway__lead">I saw it coming</p>
        <p class="takeaway__text">
          I had designed a multiple-meals flow for exactly this. It was cut for
          dev effort. The data made the case I could not.
        </p>
      </div>
      <div class="takeaway">
        <p class="takeaway__lead">The data corrected me</p>
        <p class="takeaway__text">
          I built for voice. People reached for text first, camera second. Voice,
          the input I centered on, was the smallest. Users told me something I had
          not assumed.
        </p>
      </div>
      <div class="takeaway">
        <p class="takeaway__lead">What I did with it</p>
        <p class="takeaway__text">
          I delivered this as a ranked, evidenced roadmap to the CTO and
          stakeholders. It pushed the team onto the three biggest fixes: response
          speed, batch logging, and letting Kaira answer real questions. I owned
          the problem and the direction, not the engineering that followed.
        </p>
      </div>
    </div>

    {/* 9. OUTCOME + REFLECTION */}
    <section class="beat" id="outcome">
      <h2 class="beat__h">Outcome and reflection</h2>
    </section>

    <div class="measured">
      <h3 class="measured__title">The bet, measured</h3>
      <p class="measured__sub">
        The problem I opened with was that people only logged when a coach pushed
        them. After Kaira, that flipped.
      </p>

      <dl class="measured__grid">
        <div class="rstat rstat--win">
          <dd class="rstat__value">~9 in 10</dd>
          <dt class="rstat__label">log on their own, off any diet plan</dt>
          <dd class="rstat__base">up from logging that was almost entirely coach-driven</dd>
        </div>

        <div class="rstat">
          <dd class="rstat__value">~4x</dd>
          <dt class="rstat__label">growth in monthly self-directed loggers</dt>
          <dd class="rstat__base">roughly 60 to 280 a month, Jan to Jun 2026</dd>
          <svg class="spark" viewBox="0 0 120 36" preserveAspectRatio="none" role="img" aria-label="Monthly self-directed loggers climbing from 63 in January to 277 in June 2026">
            <path class="spark__area" d="M3 33 L25.8 24.59 L48.6 18.84 L71.4 13.79 L94.2 10.57 L117 3 L117 33 L3 33 Z" />
            <path class="spark__line" d="M3 33 L25.8 24.59 L48.6 18.84 L71.4 13.79 L94.2 10.57 L117 3" />
            <circle class="spark__dot" cx="117" cy="3" r="2.4" />
          </svg>
          <div class="spark-label" aria-hidden="true"><span>Jan</span><span>Jun</span></div>
        </div>

        <div class="rstat">
          <dd class="rstat__value">1 in 4</dd>
          <dt class="rstat__label">came back to log on 5 or more separate days</dt>
          <dd class="rstat__base">without any prompt to return</dd>
        </div>
      </dl>

      <p class="measured__note">
        Figures from a backend export of roughly 900 Kaira users over about 10
        months. Directional, not final.
      </p>
    </div>

    <section class="beat">
      <p>
        Beyond the numbers, food logging became GoodFlip’s first working proof of
        an AI-first product, and a model the team could extend to everything else
        a person logs.
      </p>

      <p>
        The reality is that food logging in chronic care is not solved. What this
        built is a foundation the team could see clearly, measure honestly, and
        keep improving.
      </p>
    </section>

    <footer class="cs__foot">
      <a class="cs__back" href="/#work">← Back to work</a>
    </footer>
  </article>

  <div class="lb" data-open="false" role="dialog" aria-modal="true" aria-label="Zoomed artifact">
    <button class="lb__close" type="button" data-lb-close aria-label="Close zoom">✕</button>
    <div class="lb__stage" data-lb-stage>
      <img class="lb__img" alt="" />
    </div>
    <p class="lb__cap"></p>
  </div>
</BaseLayout>

<style is:global>
  /* This page is a light reading surface, not the orange hero. */
  body:has(.cs) {
    background-color: var(--surface);
    color: var(--ink);
  }
</style>

<style>
  .cs {
    max-width: 44rem;
    margin: 0 auto;
    padding: var(--space-2xl) var(--space-xl) var(--space-3xl);
    color: var(--ink);
  }

  .cs__back {
    display: inline-block;
    font-size: var(--font-sm);
    font-weight: 500;
    color: var(--muted);
    transition: color 0.2s ease;
  }

  .cs__back:hover {
    color: var(--accent);
  }

  /* Anchor */
  .cs__anchor {
    margin-top: var(--space-2xl);
  }

  .cs__kicker {
    font-size: var(--font-sm);
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent);
  }

  .cs__title {
    margin-top: var(--space-sm);
    font-size: clamp(2.25rem, 6vw, 3.5rem);
    font-weight: 700;
    line-height: 1.05;
    letter-spacing: -0.02em;
  }

  .cs__hook {
    margin-top: var(--space-lg);
    font-size: var(--font-lg);
    font-weight: 500;
    line-height: 1.35;
  }

  .cs__role {
    margin-top: var(--space-lg);
    font-size: var(--font-base);
    color: var(--muted);
  }

  /* Header metric cluster: two honest numbers, each with its base. */
  .cs__stats {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--space-xl);
    margin-top: var(--space-xl);
    padding-top: var(--space-lg);
    border-top: 1px solid var(--hairline);
  }

  .stat {
    display: flex;
    flex-direction: column;
  }

  .stat__value {
    order: -1;
    font-size: clamp(2rem, 4vw, 2.75rem);
    font-weight: 700;
    line-height: 1;
    letter-spacing: -0.02em;
    color: var(--ink);
  }

  .stat__label {
    margin-top: var(--space-sm);
    font-size: var(--font-base);
    line-height: 1.4;
    color: var(--ink);
    max-width: 22ch;
  }

  .stat__base {
    margin-top: var(--space-2xs);
    font-size: var(--font-xs);
    line-height: 1.4;
    color: var(--muted);
  }

  @media (max-width: 40rem) {
    .cs__stats {
      grid-template-columns: 1fr;
      gap: var(--space-lg);
    }
  }

  /* Beats */
  .beat {
    margin-top: var(--space-3xl);
  }

  .beat__eyebrow {
    font-size: var(--font-sm);
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: var(--space-xs);
  }

  .beat__h {
    font-size: var(--font-xl);
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.01em;
    margin-bottom: var(--space-md);
  }

  .beat__sub {
    font-size: var(--font-md);
    font-weight: 700;
    margin-top: var(--space-xl);
    margin-bottom: var(--space-xs);
  }

  .beat p {
    font-size: var(--font-md);
    line-height: 1.6;
    margin-top: var(--space-md);
  }

  .beat p:first-of-type {
    margin-top: 0;
  }

  .beat__lead {
    font-weight: 700;
    font-size: var(--font-lg) !important;
    line-height: 1.35;
  }

  .beat__subline {
    margin-top: var(--space-sm);
    font-size: var(--font-md);
    line-height: 1.5;
    color: var(--muted);
    max-width: 42rem;
  }

  /* The Bet: a light editorial numbered list, not boxed cards. */
  .bet-cards {
    list-style: none;
    width: min(58rem, 92vw);
    margin: var(--space-xl) 50% var(--space-2xl);
    transform: translateX(-50%);
    display: grid;
    grid-template-columns: 1fr 1fr;
    column-gap: var(--space-3xl);
    counter-reset: bet;
  }

  .bet-card {
    counter-increment: bet;
    padding: var(--space-lg) 0;
    border-top: 1px solid var(--hairline);
  }

  /* No rule above the first row. */
  .bet-card:nth-child(-n + 2) {
    border-top: 0;
    padding-top: 0;
  }

  .bet-card__label {
    position: relative;
    padding-left: 2.25rem;
    font-size: var(--font-md);
    font-weight: 700;
    line-height: 1.3;
  }

  .bet-card__label::before {
    content: counter(bet, decimal-leading-zero);
    position: absolute;
    left: 0;
    top: 0.15em;
    font-size: var(--font-sm);
    font-weight: 700;
    color: var(--accent);
  }

  .bet-card__body {
    margin-top: var(--space-xs);
    padding-left: 2.25rem;
    font-size: var(--font-sm);
    line-height: 1.5;
    color: var(--muted);
  }

  @media (max-width: 48rem) {
    .bet-cards {
      grid-template-columns: 1fr;
    }
    .bet-card:nth-child(-n + 2) {
      border-top: 1px solid var(--hairline);
      padding-top: var(--space-lg);
    }
    .bet-card:first-child {
      border-top: 0;
      padding-top: 0;
    }
  }

  /* The Pitch: three icon cards, a step up in visual weight. */
  .pitch-cards {
    list-style: none;
    width: min(60rem, 92vw);
    margin: var(--space-xl) 50% 0;
    transform: translateX(-50%);
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
  }

  .pitch-card {
    display: flex;
    flex-direction: column;
    padding: var(--space-2xl);
    background: var(--surface);
    border: 1px solid var(--hairline);
    border-radius: var(--radius-md);
    /* Subtle lift so this section reads as the designed moment. */
    box-shadow: 0 12px 32px rgba(0, 0, 0, 0.05);
  }

  .pitch-card__top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-sm);
    margin-bottom: var(--space-xl);
  }

  .pitch-card__icon {
    display: inline-flex;
    color: var(--accent);
  }

  .pitch-card__icon svg {
    width: 2.5rem;
    height: 2.5rem;
  }

  .pitch-card__tag {
    font-size: var(--font-xs);
    font-weight: 600;
    color: var(--muted);
    padding: var(--space-2xs) var(--space-sm);
    border: 1px solid var(--hairline);
    border-radius: var(--radius-pill);
  }

  .pitch-card__label {
    font-size: var(--font-lg);
    font-weight: 700;
    line-height: 1.2;
    letter-spacing: -0.01em;
  }

  .pitch-card__body {
    margin-top: var(--space-sm);
    font-size: var(--font-sm);
    line-height: 1.55;
    color: var(--muted);
  }

  @media (max-width: 48rem) {
    .pitch-cards {
      grid-template-columns: 1fr;
    }
  }

  /* Voice-study banner (Decision One) */
  .study-banner {
    width: min(64rem, 92vw);
    margin: var(--space-xl) 50% 0;
    transform: translateX(-50%);
    padding: var(--space-2xl);
    background: var(--surface);
    border: 1px solid var(--hairline);
    border-top: 3px solid var(--accent);
    border-radius: var(--radius-md);
  }

  .study-banner__eyebrow {
    font-size: var(--font-sm);
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent);
  }

  .study-banner__headline {
    margin-top: var(--space-xs);
    font-size: clamp(1.5rem, 3.5vw, 2.25rem);
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.02em;
  }

  .study-banner__setup {
    margin-top: var(--space-md);
    font-size: var(--font-md);
    line-height: 1.5;
    color: var(--muted);
    max-width: 42rem;
  }

  .study-conditions {
    display: grid;
    grid-template-columns: 1fr 1fr;
    margin-top: var(--space-xl);
    border: 1px solid var(--hairline);
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .study-cond {
    padding: var(--space-lg);
  }

  .study-cond + .study-cond {
    border-left: 1px solid var(--hairline);
  }

  .study-cond__label {
    display: inline-block;
    font-size: var(--font-xs);
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: var(--space-sm);
  }

  .study-cond__quote {
    font-size: var(--font-md);
    line-height: 1.4;
    color: var(--ink);
  }

  .study-cond__note {
    margin-top: var(--space-sm);
    font-size: var(--font-sm);
    line-height: 1.5;
    color: var(--muted);
  }

  .study-finding {
    margin-top: var(--space-xl);
    padding-left: var(--space-lg);
    border-left: 3px solid var(--accent);
    font-size: var(--font-lg);
    font-weight: 700;
    line-height: 1.35;
    color: var(--ink);
  }

  .study-aim {
    margin-top: var(--space-lg);
    font-size: var(--font-xs);
    line-height: 1.5;
    color: var(--muted);
  }

  @media (max-width: 40rem) {
    .study-banner {
      padding: var(--space-lg);
    }
    .study-conditions {
      grid-template-columns: 1fr;
    }
    .study-cond + .study-cond {
      border-left: 0;
      border-top: 1px solid var(--hairline);
    }
  }

  /* Decision Four takeaways: prose-weight, not stat cards. */
  .takeaways {
    margin-top: var(--space-2xl);
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .takeaway {
    padding-top: var(--space-lg);
    border-top: 1px solid var(--hairline);
  }

  .takeaway:first-child {
    padding-top: 0;
    border-top: 0;
  }

  .takeaway__lead {
    font-size: var(--font-sm);
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--accent);
  }

  .takeaway__text {
    margin-top: var(--space-xs);
    font-size: var(--font-md);
    line-height: 1.55;
    color: var(--ink);
    max-width: 44rem;
  }

  /* Reveal on scroll-in (opacity only, armed by JS so no-JS stays visible). */
  .reveal.is-armed {
    opacity: 0;
    transition: opacity 0.7s ease;
  }

  .reveal.is-armed.is-visible {
    opacity: 1;
  }

  @media (prefers-reduced-motion: reduce) {
    .reveal.is-armed {
      opacity: 1;
      transition: none;
    }
  }

  /* Identity: acronym reveal (section opener). */
  .acronym {
    width: min(60rem, 92vw);
    margin: var(--space-3xl) 50% var(--space-2xl);
    transform: translateX(-50%);
    text-align: center;
    padding: var(--space-xl) 0;
  }

  .acronym__mark {
    display: inline-block;
    font-size: clamp(3rem, 12vw, 7rem);
    font-weight: 900;
    letter-spacing: 0.06em;
    line-height: 1;
    /* Kaira brand gradient: indigo to teal. */
    background: linear-gradient(90deg, #4F46E5 0%, #14B8A6 100%);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  .acronym__full {
    margin-top: var(--space-lg);
    font-size: clamp(1.1rem, 3vw, 1.6rem);
    font-weight: 400;
    line-height: 1.35;
    color: var(--ink);
  }

  .acronym__full b {
    color: var(--accent);
    font-weight: 800;
  }

  /* Identity build timeline */
  .idtl {
    list-style: none;
    width: min(58rem, 92vw);
    margin: 0 50%;
    transform: translateX(-50%);
    position: relative;
    display: flex;
    flex-direction: column;
    gap: var(--space-3xl);
  }

  .idtl::before {
    content: '';
    position: absolute;
    left: 1rem;
    top: 0.75rem;
    bottom: 0.75rem;
    width: 1px;
    background: var(--hairline);
  }

  .idtl__stage {
    position: relative;
    padding-left: 3.25rem;
  }

  .idtl__num {
    position: absolute;
    left: 0;
    top: 0;
    display: grid;
    place-items: center;
    width: 2rem;
    height: 2rem;
    border-radius: 50%;
    background: var(--surface);
    border: 1px solid var(--accent);
    color: var(--accent);
    font-size: var(--font-xs);
    font-weight: 700;
  }

  .idtl__label {
    font-size: var(--font-lg);
    font-weight: 700;
    line-height: 1.2;
    letter-spacing: -0.01em;
  }

  .idtl__line {
    margin-top: var(--space-xs);
    font-size: var(--font-sm);
    line-height: 1.5;
    color: var(--muted);
    max-width: 40rem;
  }

  .idtl__visual {
    margin-top: var(--space-lg);
    border: 1px solid var(--hairline);
    border-radius: var(--radius-md);
    overflow: hidden;
    background: var(--surface);
  }

  .idtl__visual :global(img) {
    display: block;
    width: 100%;
    height: auto;
  }

  .idtl__visual--pad {
    padding: var(--space-xl);
  }

  .idtl__visual--pad :global(img) {
    max-width: 22rem;
    margin-inline: auto;
  }

  /* Repeating pattern shown as a short band, not full height. */
  .idtl__visual--band :global(img) {
    height: 11rem;
    object-fit: cover;
    object-position: center;
  }

  /* Final stage: the payoff, given the most prominence. */
  .idtl__stage--final .idtl__num {
    background: var(--accent);
    color: #fff;
  }

  .idtl__stage--final .idtl__label {
    font-size: var(--font-xl);
  }

  .idtl__visual--final {
    border-color: var(--accent);
  }

  .idtl__visual--final .shot__ph {
    min-height: 20rem;
    border: 0;
    background:
      repeating-linear-gradient(-45deg, rgba(231, 95, 56, 0.05) 0 14px, transparent 14px 28px),
      rgba(231, 95, 56, 0.04);
  }

  /* Alternate explorations: smaller and muted, so the final mark still wins. */
  .alternates {
    max-width: 34rem;
    margin-inline: auto;
    opacity: 0.85;
  }

  /* Conclusion / data table */
  .cs__wide {
    width: min(52rem, 92vw);
    margin: var(--space-xl) 50% 0;
    transform: translateX(-50%);
  }

  .ctable {
    width: 100%;
    border-collapse: collapse;
    font-size: var(--font-sm);
    line-height: 1.5;
    border: 1px solid var(--hairline);
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .ctable th,
  .ctable td {
    text-align: left;
    padding: var(--space-md);
    border-bottom: 1px solid var(--hairline);
    vertical-align: top;
  }

  .ctable th {
    font-weight: 700;
    background: rgba(0, 0, 0, 0.03);
  }

  .ctable th:first-child,
  .ctable td:first-child {
    border-right: 1px solid var(--hairline);
    width: 55%;
  }

  .ctable tr:last-child td {
    border-bottom: none;
  }

  /* Image slots (placeholders until real assets are exported) */
  .shot {
    margin: var(--space-2xl) 0;
  }

  .shot--hero {
    width: min(64rem, 92vw);
    margin-left: 50%;
    transform: translateX(-50%);
  }

  .shot__ph {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
    justify-content: center;
    padding: var(--space-2xl);
    min-height: 15rem;
    border: 1px dashed var(--hairline);
    border-radius: var(--radius-md);
    background:
      repeating-linear-gradient(-45deg, rgba(0,0,0,0.02) 0 14px, transparent 14px 28px),
      var(--surface);
  }

  .shot--hero .shot__ph {
    min-height: 22rem;
  }

  .shot__ph--proto {
    min-height: 30rem !important;
    aspect-ratio: 16 / 9;
  }

  .shot__badge {
    align-self: flex-start;
    font-size: var(--font-xs);
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--accent);
    padding: var(--space-2xs) var(--space-sm);
    border: 1px solid var(--accent);
    border-radius: var(--radius-pill);
  }

  .shot__desc {
    font-size: var(--font-sm);
    color: var(--muted);
    max-width: 40rem;
  }

  .shot figcaption {
    margin-top: var(--space-sm);
    font-size: var(--font-sm);
    color: var(--muted);
    max-width: 44rem;
    line-height: 1.5;
  }

  /* Real images inside figures */
  .shot__img {
    display: block;
    width: 100%;
    height: auto;
    border: 1px solid var(--hairline);
    border-radius: var(--radius-md);
  }

  /* Simple line diagram (thyroxine): constrain and pad on white. */
  .shot__img--pad {
    max-width: 24rem;
    margin-inline: auto;
    padding: var(--space-xl);
    background: var(--surface);
  }

  /* Tall document teaser: show the top, tap to open full. */
  .shot__img--clip {
    max-height: 28rem;
    object-fit: cover;
    object-position: top;
  }

  /* Zoomable artifact trigger */
  .zoomable {
    display: block;
    width: 100%;
    padding: 0;
    border: 0;
    background: none;
    cursor: zoom-in;
    position: relative;
  }

  .zoomable__hint {
    position: absolute;
    right: var(--space-sm);
    bottom: var(--space-sm);
    padding: var(--space-2xs) var(--space-sm);
    font-size: var(--font-xs);
    font-weight: 600;
    color: #fff;
    background: rgba(0, 0, 0, 0.6);
    border-radius: var(--radius-pill);
    opacity: 0;
    transition: opacity 0.2s ease;
    pointer-events: none;
  }

  .zoomable:hover .zoomable__hint,
  .zoomable:focus-visible .zoomable__hint {
    opacity: 1;
  }

  /* Coarse pointers can't hover: always show the hint. */
  @media (hover: none) {
    .zoomable__hint {
      opacity: 1;
    }
  }

  .expand-link {
    display: inline-block;
    margin-top: var(--space-sm);
    padding: 0;
    border: 0;
    background: none;
    font: inherit;
    font-size: var(--font-sm);
    font-weight: 600;
    color: var(--accent);
    cursor: pointer;
  }

  .expand-link:hover {
    text-decoration: underline;
  }

  /* Zoom lightbox */
  .lb {
    position: fixed;
    inset: 0;
    z-index: 2000;
    display: none;
    flex-direction: column;
    background: rgba(0, 0, 0, 0.92);
  }

  .lb[data-open='true'] {
    display: flex;
  }

  .lb__stage {
    flex: 1;
    overflow: auto;
    display: grid;
    place-items: center;
    padding: var(--space-xl);
    -webkit-overflow-scrolling: touch;
    touch-action: pinch-zoom;
  }

  .lb__img {
    max-width: 100%;
    max-height: 100%;
    object-fit: contain;
    cursor: zoom-in;
    background: #fff;
    border-radius: var(--radius-md);
  }

  /* Zoomed: render at natural size so the stage scrolls/pans. */
  .lb__img.is-zoomed {
    max-width: none;
    max-height: none;
    width: auto;
    cursor: zoom-out;
  }

  .lb__close {
    position: absolute;
    top: var(--space-md);
    right: var(--space-md);
    z-index: 1;
    display: grid;
    place-items: center;
    width: 2.75rem;
    height: 2.75rem;
    font-size: var(--font-lg);
    color: #fff;
    background: rgba(255, 255, 255, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.25);
    border-radius: 50%;
    cursor: pointer;
  }

  .lb__cap {
    flex-shrink: 0;
    padding: var(--space-md) var(--space-xl) var(--space-lg);
    text-align: center;
    font-size: var(--font-sm);
    line-height: 1.5;
    color: rgba(255, 255, 255, 0.85);
    max-width: 52rem;
    margin-inline: auto;
  }

  .shot__strip {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-sm);
  }

  .shot__strip :global(img) {
    width: 100%;
    height: 100%;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    border: 1px solid var(--hairline);
    border-radius: calc(var(--radius-md) - 6px);
  }

  /* Figma prototype embed */
  .proto {
    aspect-ratio: 4 / 3;
    width: 100%;
    overflow: hidden;
    border: 1px solid var(--hairline);
    border-radius: var(--radius-md);
    background: rgba(0, 0, 0, 0.03);
  }

  .proto__frame {
    display: block;
    width: 100%;
    height: 100%;
    border: 0;
  }

  /* Research impact panel: a contained "pause and look" band. */
  .research {
    width: min(60rem, 92vw);
    margin: var(--space-2xl) 50% 0;
    transform: translateX(-50%);
    padding: var(--space-2xl);
    background: rgba(0, 0, 0, 0.035);
    border: 1px solid var(--hairline);
    border-radius: var(--radius-md);
  }

  .research__eyebrow {
    font-size: var(--font-sm);
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--accent);
  }

  .research__title {
    margin-top: var(--space-xs);
    font-size: var(--font-xl);
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.01em;
  }

  .research__lede {
    margin-top: var(--space-sm);
    font-size: var(--font-md);
    color: var(--ink);
    max-width: 40rem;
  }

  .research__method {
    list-style: none;
    margin-top: var(--space-lg);
    padding-top: var(--space-md);
    border-top: 1px solid var(--hairline);
    display: flex;
    flex-direction: column;
    gap: var(--space-2xs);
    font-size: var(--font-xs);
    color: var(--muted);
    line-height: 1.5;
  }

  .research__grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(10rem, 1fr));
    gap: var(--space-md);
    margin-top: var(--space-xl);
  }

  .rstat {
    display: flex;
    flex-direction: column;
    padding: var(--space-md);
    background: var(--surface);
    border: 1px solid var(--hairline);
    border-radius: calc(var(--radius-md) - 4px);
  }

  .rstat__value {
    order: -1;
    font-size: clamp(1.75rem, 3.5vw, 2.25rem);
    font-weight: 700;
    line-height: 1;
    letter-spacing: -0.02em;
    color: var(--ink);
  }

  .rstat__label {
    margin-top: var(--space-sm);
    font-size: var(--font-sm);
    line-height: 1.35;
    color: var(--ink);
  }

  .rstat__base {
    margin-top: var(--space-xs);
    font-size: var(--font-xs);
    line-height: 1.4;
    color: var(--muted);
  }

  /* The one earned win, set apart with the accent token. */
  .rstat--win {
    border-color: var(--accent);
    background: rgba(231, 95, 56, 0.06);
  }

  .rstat--win .rstat__value {
    color: var(--accent);
  }

  /* Case-study hero banner (top of page) */
  .cs__hero {
    width: min(64rem, 92vw);
    margin: var(--space-lg) 50% var(--space-2xl);
    transform: translateX(-50%);
    border: 1px solid var(--hairline);
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .cs__hero :global(img) {
    display: block;
    width: 100%;
    height: auto;
  }

  /* "The bet, measured" block reuses the .rstat card styles. */
  .measured {
    width: min(56rem, 92vw);
    margin: var(--space-xl) 50% 0;
    transform: translateX(-50%);
  }

  .measured__title {
    font-size: var(--font-lg);
    font-weight: 700;
    letter-spacing: -0.01em;
  }

  .measured__sub {
    margin-top: var(--space-xs);
    font-size: var(--font-md);
    line-height: 1.5;
    color: var(--muted);
    max-width: 42rem;
  }

  .measured__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-md);
    margin-top: var(--space-xl);
  }

  .measured__note {
    margin-top: var(--space-lg);
    font-size: var(--font-xs);
    line-height: 1.5;
    color: var(--muted);
  }

  /* Monthly-growth sparkline (six real points). */
  .spark {
    display: block;
    width: 100%;
    height: 2.75rem;
    margin-top: var(--space-md);
    overflow: visible;
  }

  .spark__line {
    fill: none;
    stroke: var(--accent);
    stroke-width: 1.75;
    vector-effect: non-scaling-stroke;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .spark__area {
    fill: rgba(231, 95, 56, 0.08);
    stroke: none;
  }

  .spark__dot {
    fill: var(--accent);
  }

  .spark-label {
    display: flex;
    justify-content: space-between;
    margin-top: var(--space-2xs);
    font-size: var(--font-xs);
    color: var(--muted);
  }

  /* Callback link on the first bet reason. */
  .bet-card__callback {
    display: inline-block;
    margin-top: var(--space-sm);
    padding-left: 2.25rem;
    font-size: var(--font-xs);
    line-height: 1.4;
    color: var(--muted);
    text-decoration: underline;
    text-underline-offset: 2px;
    text-decoration-color: var(--hairline);
    transition: color 0.2s ease;
  }

  .bet-card__callback:hover {
    color: var(--accent);
    text-decoration-color: var(--accent);
  }

  @media (max-width: 44rem) {
    .measured__grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 40rem) {
    .research {
      padding: var(--space-lg);
    }
    .shot__strip {
      gap: var(--space-xs);
    }
  }

  /* Fill slots (content Daksh still owes) */
  .fill {
    margin-top: var(--space-lg);
    padding: var(--space-lg);
    border-left: 3px solid var(--accent);
    background: rgba(231, 95, 56, 0.06);
    border-radius: 0 var(--radius-md) var(--radius-md) 0;
  }

  /* Confirm slots (facts Daksh needs to verify) render in a cooler neutral. */
  .fill--confirm {
    border-left-color: var(--muted);
    background: rgba(0, 0, 0, 0.04);
  }

  .fill__tag {
    display: inline-block;
    font-size: var(--font-xs);
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: var(--space-xs);
  }

  .fill--confirm .fill__tag {
    color: var(--muted);
  }

  .fill p {
    font-size: var(--font-sm);
    line-height: 1.5;
    color: var(--muted);
  }

  .cs__foot {
    margin-top: var(--space-3xl);
    padding-top: var(--space-lg);
    border-top: 1px solid var(--hairline);
  }
</style>

<script>
  const lb = document.querySelector<HTMLElement>('.lb');
  if (lb) {
    const img = lb.querySelector<HTMLImageElement>('.lb__img')!;
    const cap = lb.querySelector<HTMLElement>('.lb__cap')!;
    const stage = lb.querySelector<HTMLElement>('[data-lb-stage]')!;

    function open(src: string, caption: string) {
      img.src = src;
      img.classList.remove('is-zoomed');
      cap.textContent = caption;
      lb.dataset.open = 'true';
      document.body.style.overflow = 'hidden';
    }
    function close() {
      lb.dataset.open = 'false';
      img.removeAttribute('src');
      document.body.style.overflow = '';
    }

    document.querySelectorAll<HTMLElement>('[data-lightbox]').forEach((el) => {
      el.addEventListener('click', (e) => {
        e.preventDefault();
        open(el.dataset.lightbox || '', el.dataset.caption || '');
      });
    });

    img.addEventListener('click', () => img.classList.toggle('is-zoomed'));
    stage.addEventListener('click', (e) => {
      if (e.target === stage) close();
    });
    lb.querySelector('[data-lb-close]')!.addEventListener('click', close);
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && lb.dataset.open === 'true') close();
    });
  }

  // Reveal-on-scroll. Arm via JS so content stays visible without JS.
  const reveals = document.querySelectorAll<HTMLElement>('.reveal');
  if (reveals.length && 'IntersectionObserver' in window) {
    const io = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            io.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.15 },
    );
    reveals.forEach((el) => {
      el.classList.add('is-armed');
      io.observe(el);
    });
    // Safety net: never leave content hidden if the observer does not fire.
    window.setTimeout(() => {
      reveals.forEach((el) => el.classList.add('is-visible'));
    }, 2500);
  }
</script>
